services:
  - type: web
    name: melanoma-pipeline
    runtime: docker
    dockerfilePath: ./web_app/Dockerfile
    dockerContext: .
    plan: free
    envVars:
      - key: PORT
        value: 8000
    healthCheckPath: /health
