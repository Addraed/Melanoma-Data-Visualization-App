services:
  - type: web
    name: melanoma-pipeline
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
    envVars:
      - key: PORT
        value: 8000
    healthCheckPath: /health
